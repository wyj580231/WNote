import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: { flex: 1 },
  themeBlock: {
    width: 30,
  },
  themeContainer: {
    backgroundColor: '#fff',
  },
  themeItem: {
    backgroundColor: '#fff',
    borderWidth: 0,
  },
});
