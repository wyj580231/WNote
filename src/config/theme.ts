import { Theme } from 'react-native-elements';

const theme: Theme = {
  Header: {
    containerStyle: {
      backgroundColor: '#fff',
      height: 50,
      paddingTop: 0,
    },
  },
};
export default theme;
